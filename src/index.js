// src/index.js
import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './REGISTRO CETRAQ'; // Importa tu componente principal
// Puedes comentar estas líneas si no tienes el archivo.
// import './index.css'; 

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
